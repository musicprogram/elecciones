<%= simple_form_for(@representantet, :html => { :class => 'form-horizontal' } ) do |f| %>
  <% if @representantet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@representantet.errors.count, "error") %> prohibited this representantet from being saved:</h2>

      <ul>
      <% @representantet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <label>Foto</label><br>
    <%= f.file_field :foto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
