<%= simple_form_for(@representanten, :html => { :class => 'form-horizontal' } ) do |f| %>
  <% if @representanten.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@representanten.errors.count, "error") %> prohibited this representanten from being saved:</h2>

      <ul>
      <% @representanten.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <label>Foto</label><br>
    <%= f.file_field :foto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
