<%= current_usuario.email %>
<center>
<h1>Lista de postulados Jornada Mañana</h1>
<div class="contene">
<div class="panel panel-default">
  
  <div class="panel-body">
    <% @representantems.each do |representantem| %>
      <div class="contene2">
        <div class="panel panel-primary">
          <div class="panel-heading">
            <h3 class="panel-title"><%= representantem.nombre %></h3>
          </div>
          <div class="panel-body">
            
            <%= image_tag(representantem.foto,  height: '250', width: '250') %>
            
            <div class="panel-footer"><%= link_to 'Editar', edit_representantem_path(representantem) %>
            <%= link_to 'Destroy', representantem, method: :delete, data: { confirm: 'Are you sure?' } %>
            <%= link_to like_representantem_path(representantem), method: :put, class: "btn btn-default"  do %>
              <i></i>
              <%= representantem.get_upvotes.size %>
            <% end %>
            </div>
            
          </div>
        </div>
      </div>
    <% end %>
      </div>
  </div>
</div>


<br>

<%= button_to 'New Representantem', new_representantem_path, class: "btn btn-info", :method => "get"%>
</center>